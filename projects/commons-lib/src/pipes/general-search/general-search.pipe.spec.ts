import { GeneralSearchPipe } from './general-search.pipe';

describe('GeneralSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new GeneralSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
